# 5. 로드맵 · 리스크

## 마일스톤

기간은 **주말·저녁 시간 기준의 1인 개발** 가정입니다. 각 단계 끝에 실제로 쓸 수 있는
빌드가 나오는 것을 원칙으로 합니다.

### M0 — 스캐폴딩 (1주)
- Xcode 프로젝트 + 로컬 SPM 패키지 골격 (`Core`, `Persistence`, `DesignSystem`, `Features`)
- SwiftData 스키마 v1 + 인메모리 컨테이너 테스트
- CloudKit 컨테이너 설정, 기기 2대 동기화 확인
- `Money` / `Quantity` 값 타입 + 포맷터 + 테스트
- **검증 과제**: 스케일드 정수가 CloudKit 왕복에서 정확히 보존되는지 확인 (→ ADR-0003)
- CI: `Core` 패키지 테스트 자동 실행

### M1 — 주간 점검 루틴 (2~3주) ★ 첫 실사용
- 구성원 / 계좌 / 보유 자산 CRUD, 입력 주기 태그(매주/월 1회/고정)
- **주간 점검 연속 입력 화면** — 그룹 진행률, 지난주 값·증감, 키패드 체이닝
- **토요일 알림** + 미입력 시 일요일 재알림 + 총액 빠른 입력
- **점검 완료 화면** — 이번 주 변화, 연속 기록 주차, 구성원별 증감
- 주간 스냅샷 기록
- 현황판: 총자산, 이번 주 점검 바, 구성원 카드, 자산군·지역 비중
- ~~CSV 가져오기~~ → **지난 기록 직접 입력**(더보기)으로 대체. 몇 줄을 옮기는 일이면 파일 형식을 맞추는 것보다 손으로 넣는 편이 빠르고, 틀린 값이 조용히 들어오지 않는다
- ✅ **완료 기준: 토요일 알림에서 시작해 3분 안에 점검을 끝낼 수 있다**

### M2 — 궤적과 예측 (2~3주) ★ 이 앱의 정체성
- ✅ 적립 계획(구성원별 배분 포함) / 연금 / 목돈 이벤트 입력
- ✅ 은퇴 후 인출 구간과 자산 고갈 시점
- 결정론적 프로젝션 엔진 + 테스트
- 순자산 궤적 차트 (과거 실선 + 미래 점선)
- ✅ 로드맵 타임라인 + 마일스톤(직접 찍기 + 자동 판정)
- **계획선 대비 실적선** — 점검 완료 화면의 12주 비교 차트 포함
- 실질/명목 토글
- ✅ **완료 기준: 이번 주 숫자가 계획선 위인지 아래인지 한눈에 보인다**

### M3 — 시뮬레이션 (1~2주)
- ✅ 몬테카를로(시드 고정 SplitMix64 + Box–Muller) + 신뢰구간 밴드 + 목표 달성 확률
- ✅ What-if 슬라이더 4개 (매월 적립 / 은퇴 연도 / 연 기대수익률 / 연 변동성),
  120ms 디바운스 후 `Task.detached` 에서 계산
- ✅ 계획 대비 델타, 분포 3줄, `[계획에 반영]`
- ✅ 자산 고갈 나이 — 연금 소득 모델과 함께 들어왔다
- ✅ 시나리오 저장·비교 (손잡이 조합에 이름을 붙여 둔다. 저장은 계획을 바꾸지 않는다)
- ✅ **완료 기준: 월 적립 50만원 증가의 효과를 3초 안에 확인할 수 있다**

### M4 — 점검 (1~2주)
- ✅ **자산 진단 여섯 규칙** — 4% 규칙 · 부동산 비중 · 국가 배분 · 세제혜택 계좌 순서 ·
  72의 법칙 · 선저축 비율 ([설계 2.8](02-screens.md#28-자산-진단-더보기--자산-진단-현황판에-요약))
- ✅ 기준은 전부 사용자가 바꾼다. 계좌 한도는 계좌 화면에서 직접 넣는다
- ✅ 세적(PFIC) 제약 경고, 현황판 경고 스트립
- ✅ 현황판 진단 요약 (조치 · 주의 · 지킴)
- ☐ 규칙 추가·삭제, 채우는 순서 직접 바꾸기
- ✅ 유의사항·할 일 + 기한 로컬 알림 (주간 점검 알림과 따로 건다)
- ✅ **완료 기준: 분기 점검을 앱 안에서 끝낼 수 있다**

### M5 — 마감 (1~2주)
- ✅ 알림 설정 화면, 온보딩 흐름, 빈 상태 화면
- ✅ Face ID 잠금 · 금액 가리기 · 알림 금액 노출 설정 (셋을 따로 둔다 — 목적이 다르다)
- ✅ 1페이지 이미지 내보내기 (`ImageRenderer`)
- ✅ CSV 내보내기 (백업). 가져오기는 만들지 않는다 — 사용자가 직접 넣기로 했다
- ✅ **다중 통화는 만들지 않는다** — 사용자가 해외 종목도 원화로 환산해서 적는다.
  환율을 앱이 다루면 "내가 안 한 변화"가 증감에 섞이고, 환율을 안 넣으면 합계가
  조용히 비는 함정이 생긴다. 모든 금액은 원화로 통일한다 (ADR-0005)
- ☐ 접근성 점검, 한국어 문자열 정리 — 실기기에서 VoiceOver·Dynamic Type 을
  켜 보고 고쳐야 의미가 있다. 원격 세션에서 짐작으로 만지면 오히려 어긋난다
- ✅ **완료 기준: 가족에게 보여줄 수 있다**

### M6 — 출시
- TestFlight 내부 테스트, 개인정보 처리방침, App Privacy 신고
- App Store 심사 대응 (금융 카테고리 고지 문구 확인)

### M7 — iPad 확장 (iPhone 버전이 안정된 뒤)
**사용자가 명시적으로 계획한 다음 단계입니다.** iPhone 버전을 어느 정도 완성도 있게
만든 뒤 iPad로 확장합니다. 착수 조건은 "M5까지 끝나고 몇 주 실제로 써 봤을 때"입니다.

- `TARGETED_DEVICE_FAMILY` 를 `"1"` → `"1,2"` 로 (project.yml)
- 현황판을 **1페이지 원본 비율 그대로** 펼친다 — iPad가 원본 A4 레이아웃에 가장 가깝다.
  구성원 카드 4개를 2×2 그리드로 두고 궤적을 크게 쓰는 구성.
- `NavigationSplitView` — 사이드바(구성원·계획·점검) + 상세
- 주간 점검은 iPad에서 하드웨어 키보드 Tab 이동을 지원 (입력 속도가 더 중요해진다)
- 화면 코드가 iPhone 레이아웃에 못 박히지 않도록, M1부터 고정 폭 대신
  `ViewThatFits`/가변 레이아웃을 쓰고 화면별 최대 폭만 제한한다

### 그 이후
위젯 → 배우자 실시간 공유(CloudKit shared DB) → 리밸런싱 제안 →
입력 보조(증감률만 입력·스크린샷 인식) → 세금 반영 → Mac

## 검증이 필요한 가정

설계를 확정하기 전에 M0에서 직접 확인해야 하는 것들입니다.

| # | 가정 | 확인 방법 |
|---|---|---|
| A1 | SwiftData + CloudKit이 이 스키마 규모(12개 모델, 다대일 관계 다수)에서 안정적이다 | M0에서 실기기 2대 동기화 테스트 |
| A2 | 스케일드 `Int`가 CloudKit 왕복에서 손실 없이 보존된다 | M0 왕복 테스트 |
| A3 | 몬테카를로 1,000경로 × 30년(360스텝)이 아이폰에서 200ms 이내 | M0에서 프로토타입 벤치마크 |
| A4 | 토요일 알림이 실제로 도달하고, 사용자가 그것을 계기로 앱을 연다 | M1 이후 4주간 실사용 |
| A5 | 주간 점검으로 자산 24건 입력이 3분 이내에 끝난다 | M1 완료 후 실측 |

## 리스크

### R1 — SwiftData + CloudKit 제약 (영향 高 / 가능성 高)
CloudKit 미러링을 켜면 `@Attribute(.unique)` 사용 불가, 모든 속성에 기본값 필요,
모든 관계가 옵셔널이어야 합니다. 나중에 켜면 스키마를 뒤엎어야 합니다.

**대응**: 처음부터 제약을 지킨 스키마로 설계하고 M0에서 실기기 동기화를 확인합니다.
유일성은 앱 레벨에서 보장합니다. → [ADR-0001](adr/0001-swiftdata-cloudkit.md)

### R2 — 주간 루틴이 끊긴다 (영향 極大 / 가능성 高)
**이 앱의 가장 큰 리스크입니다.** 모든 데이터가 수동 입력이므로 점검을 몇 주 거르면
궤적에 구멍이 생기고, 구멍이 생기면 계획 대비 실적이 의미를 잃고, 그러면 앱을 안 엽니다.

**대응 (M1에 전부 포함)**:
- 알림이 유일한 트리거이므로 권한 거부·재알림·시간대 변경을 방어적으로 다룹니다.
- 총액 하나만 적어도 그 주가 기록되게 합니다 — 완벽하지 않아도 이어지는 것이 중요합니다.
- 끝낸 직후 보상(계획 대비 실적, 연속 기록 주차)을 보여줍니다.
- 끊겼을 때 나무라지 않습니다. 죄책감을 주는 UI는 앱을 지우게 만듭니다.
- 4주간 실제로 써 보고 이탈 지점을 관찰합니다(A4).

### R3 — 예측의 신뢰성 (영향 高 / 가능성 中)
연 8% 복리로 23년을 그리면 숫자가 매우 커집니다. 사용자가 이를 **약속으로 받아들이면**
잘못된 재무 결정으로 이어질 수 있습니다.

**대응**: 신뢰구간 밴드를 기본으로 보여주고(단일 선이 아니라), 실질가치 토글을 눈에 띄게 두고,
모든 예측 화면에 고지 문구를 둡니다. 보수 시나리오를 기본 시나리오와 나란히 보여줍니다.

### R4 — 입력 항목이 불어난다 (영향 中 / 가능성 中)
자산이 늘수록 주간 점검이 길어지고, 길어지면 R2로 이어집니다.

**대응**: 입력 주기 태그(`매주`/`월 1회`/`고정`)로 매주 물어보는 항목을 최소로 유지합니다.
전세보증금처럼 안 바뀌는 항목은 아예 건너뜁니다. 점검 소요 시간을 기록해 두고
3분을 넘기기 시작하면 항목을 정리하도록 안내합니다.

### R5 — 개인 금융 데이터 유출 (영향 極大 / 가능성 低)
가족 4인의 자산·계좌·세적 정보가 담깁니다.

**대응**: 서버를 두지 않는 것이 1차 방어입니다. 앱 잠금, 앱 전환기 블러,
릴리즈 빌드 로깅 금지, 크래시 리포트 제외를 M5에 포함합니다.
**저장소(GitHub)에도 실제 금액·기관명을 커밋하지 않습니다** — 테스트 픽스처는 가상 데이터를 씁니다.

### R6 — 규제 경계 (영향 中 / 가능성 低)
투자 추천이나 수익 보장으로 해석될 여지가 있으면 문제가 됩니다.

**대응**: 앱은 사용자가 입력한 가정을 계산만 합니다. 종목·상품을 제안하지 않습니다.
세무 관련은 기한 알림까지만 하고 세액을 확정 계산하지 않습니다.

### R7 — 범위 팽창 (영향 中 / 가능성 高)
가계부, 리밸런싱, 세금 계산, 자녀 교육비, 부동산 시세… 넣고 싶은 것이 계속 생깁니다.

**대응**: 모든 기능 제안을 *"기존 1페이지를 더 잘 만들어 주는가?"* 로 판정합니다.
[Non-goals](01-product-spec.md#18-범위-밖-non-goals) 목록을 실제로 사용합니다.
