import type { Source } from "./Core";
/**
 * A callbag source that never emits.
 *
 * Adapted from https://github.com/Andarist/callbag-never
 *
 * Marble diagram:
 *   {never}
 *   "-----"
 *
 * @param sourceArgs[0] Must be START.
 * @param sourceArgs[1] A talkback to communicate with the callbag sink.
 *
 * @internal
 */
export declare const never: Source<never, never>;
