import type { Source } from "./Core";
/**
 * Callbag operator that creates an error and emits it
 * to the sink immediately upon subscription.
 *
 * Marble diagram:
 *   {throwError}
 *   "         #"
 *
 * @param error The error to end the callbag stream with.
 * @returns A callbag source that ends with the
 *     error indicated.
 *
 * @internal
 */
export declare const throwError: <E>(error: E) => Source<never, E>;
