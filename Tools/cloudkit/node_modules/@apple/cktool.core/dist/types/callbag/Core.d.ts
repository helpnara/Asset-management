export declare type START = 0;
export declare const START: START;
export declare type DATA = 1;
export declare const DATA: DATA;
export declare type END = 2;
export declare const END: END;
/**
 * Callbag call types.
 * @internal
 */
export declare type CallbagType = START | DATA | END;
export declare type SourceArgs<T, E> = [type: START, talkback: Sink<T, E>] | [type: DATA] | [type: END];
/**
 * A callbag stream that emits.
 * @internal
 */
export interface Source<T, E> {
    (...args: SourceArgs<T, E>): void;
}
export declare type SinkArgs<T, E> = [type: START, talkback: Source<T, E>] | [type: DATA, data: T] | [type: END, error?: E];
/**
 * A receiver of callbag stream emits.
 * @internal
 */
export interface Sink<T, E> {
    (...args: SinkArgs<T, E>): void;
}
export declare type SourceTalkbackArgs<T, E> = SinkArgs<T, E>;
/**
 * A function that takes a callbag source and
 * returns another callbag source.
 *
 * @internal
 */
export declare type PipeableOperator<T, U, TE, UE> = (source: Source<T, TE>) => Source<U, UE>;
/**
 * For the given callbag operator, return the item
 * type from the input source.
 *
 * @internal
 */
export declare type OperatorInput<T extends PipeableOperator<any, any, any, any>> = T extends PipeableOperator<infer U, any, any, any> ? U : never;
/**
 * For the given callbag operator, return the error
 * type from the input source.
 *
 * @internal
 */
export declare type OperatorInputError<T extends PipeableOperator<any, any, any, any>> = T extends PipeableOperator<any, any, infer U, any> ? U : never;
/**
 * For the given callbag operator, return the item
 * type from the output source.
 *
 * @internal
 */
export declare type OperatorOutput<T extends PipeableOperator<any, any, any, any>> = T extends PipeableOperator<any, infer U, any, any> ? U : never;
/**
 * For the given callbag operator, return the error
 * type from the output source.
 *
 * @internal
 */
export declare type OperatorOutputError<T extends PipeableOperator<any, any, any, any>> = T extends PipeableOperator<any, any, any, infer U> ? U : never;
/**
 * A consumer of a callbag source.
 *
 * @internal
 */
export declare type Consumer<T, E, R> = (source: Source<T, E>) => R;
