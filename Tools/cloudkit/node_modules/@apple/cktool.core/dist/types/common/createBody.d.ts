/// <reference lib="dom" />
import type { ValueFromValidator } from "../validation";
import type { CreateFormDataFn } from "./Configuration";
import type { PrivateApiOperation } from "./PrivateApiOperation";
/**
 * Creates a form data object to be used when calling the
 * operation end point.
 *
 * @param createFormDataFn A function that creates
 *     platform-specific form data objects.
 * @param isBlob A function that returns true, if and
 *     only if, the test value is a Blob.
 * @param operation The descriptor for the operation.
 * @param params Form data parameters to be included in
 *     the resultant form data object.
 * @returns A form data object containing the data passed
 *     to this function, if able. An exception is thrown
 *     otherwise.
 */
export declare function createBody<OP extends PrivateApiOperation>(createFormDataFn: CreateFormDataFn, isBlob: (value: unknown) => value is Blob, operation: OP, params?: ValueFromValidator<ReturnType<OP["getParamsValidator"]>>): ReturnType<CreateFormDataFn> | undefined;
