import type { Source } from "../callbag";
import { CancellablePromise } from "./CancellablePromise";
import type { ReactiveStreamError } from "./Errors";
import { CancelledError } from "./Errors";
/**
 * A function to convert a callbag source to a cancellable promise.
 * The callbag source should emit only one item of data before it
 * completes. If it does not do so, the promise will reject.
 *
 * @param source A pullable (not listenable) source that
 *     emits a single item only when requested to do so.
 */
export declare function pullableToCancellablePromise<T, E>(source: Source<T, E>): CancellablePromise<T, E | CancelledError | ReactiveStreamError>;
