import type { Source } from "../callbag";
import type { FetchError, InvalidSecuritySchemeError } from "./Errors";
import type { RequestOptions } from "./RequestOptions";
import type { ResponseStreamItem } from "./ResponseStreamItem";
/**
 * A function used to send the given request to the server.
 *
 * A pullable callbag source is returned in order
 * to allow the request to be made lazily.
 *
 * @param requestOptions Options for the HTTP(S) request.
 * @returns A pullable callbag source that emits
 *     {@link ResponseStreamItem} objects.
 */
export declare type RequestFunction = (requestOptions: RequestOptions) => Source<ResponseStreamItem, FetchError | InvalidSecuritySchemeError>;
