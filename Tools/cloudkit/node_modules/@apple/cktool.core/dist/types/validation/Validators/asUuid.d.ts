import { ValidatedNEA } from "../Validated";
import { ValueNotUuidError } from "../ValidationErrors";
import type { ConvertibleToUuid, Uuid } from "./Types";
/**
 * A validator that returns {@link Validated} containing
 * the tested value, if the value is a UUID.
 * If the value passed in is not a UUID value, then
 * {@link Invalid} will be returned with an error of
 * {@link ValueNotUuidError}.
 *
 * @internal
 */
export declare const asUuid: (value: ConvertibleToUuid) => ValidatedNEA<ValueNotUuidError, Uuid>;
/**
 * A validator that returns {@link Validated} containing
 * `none`, if the value passed in is `undefined`,
 * `null` or `none`.
 * Otherwise, returns the result of the value passed
 * to {@link asUuid} wrapped in `Some`.
 *
 * @internal
 */
export declare const asOptionalUuid: (value: string | import("fp-ts/lib/Option").Option<string> | null | undefined) => ValidatedNEA<ValueNotUuidError, import("fp-ts/lib/Option").Option<Uuid>>;
/**
 * Attempt to coerce the value passed in to an {@link Uuid}
 * value. If the value does not pass validation
 * a {@link ValueNotUuidError} exception will be thrown.
 *
 * @param value A UUID string value.
 * @returns A UUID string value validated to be an
 *     {@link Uuid} value.
 */
export declare const toUuid: (value: ConvertibleToUuid) => Uuid;
/**
 * Generate a UUID.
 *
 * @returns Returns a new UUID string.
 */
export declare const createUuid: (name?: string | undefined) => Uuid;
