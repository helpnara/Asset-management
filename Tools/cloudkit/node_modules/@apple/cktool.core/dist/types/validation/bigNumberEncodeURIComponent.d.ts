import type BigNumber from "bignumber.js";
/**
 * Encodes a value as a valid component of a Uniform Resource Identifier (URI).
 * This extends the JS version of {@link encodeURIComponent} to be aware
 * of bigint and {@link BigNumber} values.
 *
 * @param uriComponent A value representing a URI component to encode.
 * @returns The encoded URI component.
 */
export declare function bigNumberEncodeURIComponent(uriComponent: string | number | boolean | bigint | BigNumber): string;
