import { ValueNotStringError } from "../ValidationErrors";
import type { ValidatedNEA } from "../Validated";
/**
 * A validator that returns {@link Validated} containing
 * the tested value, if the value is a string.
 * If the value passed in is not a string value, then
 * {@link Invalid} will be returned with an error of
 * {@link ValueNotStringError}.
 *
 * @internal
 */
export declare const asString: (value: string) => ValidatedNEA<ValueNotStringError, string>;
/**
 * A validator that returns {@link Validated} containing
 * `none`, if the value passed in is `undefined`,
 * `null` or `none`.
 * Otherwise, returns the result of the value passed
 * to {@link asString} wrapped in `Some`.
 * @internal
 */
export declare const asOptionalString: (value: string | import("fp-ts/lib/Option").Option<string> | null | undefined) => ValidatedNEA<ValueNotStringError, import("fp-ts/lib/Option").Option<string>>;
