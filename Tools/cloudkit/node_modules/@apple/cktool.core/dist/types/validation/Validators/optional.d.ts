import type { Option } from "fp-ts/Option";
import { ValidatedNEA } from "../Validated";
import { ValidatorNEA } from "./Types";
/**
 * A function that creates a validator that returns
 * {@link Validated} containing the tested value, if the value
 * is undefined, null, or the value succeeds against
 * nonOptionalValidator.
 *
 * @param nonOptionalValidator The validator that will be used
 *     if the value is defined.
 *
 * @internal
 */
export declare const optional: <E, T, U>(nonOptionalValidator: ValidatorNEA<E, T, U>) => (value: T | Option<T> | null | undefined) => ValidatedNEA<E, Option<U>>;
