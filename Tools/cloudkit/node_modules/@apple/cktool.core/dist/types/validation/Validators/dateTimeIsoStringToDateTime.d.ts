import { ValueNotDateStringError, ValueNotStringError } from "../ValidationErrors";
import type { ValidatedNEA } from "../Validated";
/**
 * Attempts to convert an ISO date string to a Date object.
 * Intended to be used when parsing a JSON date string to a
 * local Date value.
 *
 * @internal
 */
export declare const dateTimeIsoStringToDateTime: (value: string) => ValidatedNEA<ValueNotDateStringError | ValueNotStringError, Date>;
