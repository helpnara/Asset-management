import { ValueNotDateError } from "../ValidationErrors";
import { ValidatedNEA } from "../Validated";
import { IsoDateString } from "./Types";
/**
 * Attempts to coerce a date value to an ISO date string.
 * Intended to be used when encoding a local Date object
 * to an ISO date string in JSON.
 *
 * @internal
 */
export declare const dateTimeToIsoString: (value: Date) => ValidatedNEA<ValueNotDateError, IsoDateString>;
