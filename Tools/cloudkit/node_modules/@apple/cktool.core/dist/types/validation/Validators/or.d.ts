import type { ValidatedNEA } from "../Validated";
import { ValueInvalidError } from "../ValidationErrors";
import { ValidatorNEA, ValueFromValidatorNEAs } from "./Types";
/**
 * Creates a function takes several validators and passes the
 * value to test to each validator. If any validator returns
 * valid, then the function will return the result of the validator
 * that passed.
 *
 * @internal
 */
export declare const or: <VS extends ValidatorNEA<unknown, any, unknown>[]>(...validators: VS) => (val: unknown) => ValidatedNEA<ValueInvalidError, ValueFromValidatorNEAs<VS>>;
