import type { ValidatedNEA } from "../Validated";
import { InvalidObjectError, ValueNotObjectError } from "../ValidationErrors";
import type { UnwrappedValueFromValidator } from "./ObjectTypes";
import type { ValidatorNEA } from "./Types";
declare type UnwrappedValues<TValidators extends Record<string, ValidatorNEA>> = {
    [K in keyof TValidators]: UnwrappedValueFromValidator<TValidators[K]>;
};
/**
 * Returns a validator that takes a value and determines
 * whether or not it can be transformed into an object of
 * the specified type.
 * The success value of this function is an object that
 * conforms to T.
 *
 * Use this function when you want the success type to be
 * determined by the generic parameter to this function.
 *
 * @param propertyValidators A dictionary of validators
 *     to be applied to
 *     values of the object passed in to the validator.
 *     If the object has a key that does not appear in the
 *     validator dictionary, the value is ignored.
 * @internal
 */
export declare function asObject<TValidators extends Record<string, ValidatorNEA> = Record<string, ValidatorNEA>>(propertyValidators: TValidators): <T>(maybeObject: T) => ValidatedNEA<ValueNotObjectError | InvalidObjectError, UnwrappedValues<TValidators>>;
export {};
