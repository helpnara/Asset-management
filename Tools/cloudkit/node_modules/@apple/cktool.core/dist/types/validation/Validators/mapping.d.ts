import { ValueNotObjectError, ValueTypeNotRecognizedError } from "../ValidationErrors";
import type { ValidatedNEA } from "../Validated";
import { ValidatorNEA } from "./Types";
/**
 * Creates a validator that validates that an object
 * with a discriminator value, uses the appropriate
 * validator for that object and takes the value
 * passed in and returns a Validated object containing
 * the tested value.
 * Intended to be used to encode a local value to JSON.
 *
 * @internal
 */
export declare const encoderMapping: ({ discriminator, encoderMap, }: {
    discriminator: string;
    encoderMap: Map<string, () => ValidatorNEA>;
}) => (value: unknown) => ValidatedNEA<ValueNotObjectError | ValueTypeNotRecognizedError, unknown>;
/**
 * Creates a validator that validates that an object
 * with a discriminator value, uses the appropriate
 * validator for that object and takes the value
 * passed in and returns a Validated object containing
 * the tested value.
 * Intended to be used to decode JSON to a validated
 * local value conforming to the type as described.
 *
 * @internal
 */
export declare const decoderMapping: <U>({ discriminator, decoderMap, }: {
    discriminator: string;
    decoderMap: Map<string, () => ValidatorNEA<unknown, unknown, U>>;
}) => (value: unknown) => ValidatedNEA<ValueTypeNotRecognizedError | ValueNotObjectError, U>;
