import { ValueNotExpected } from "../ValidationErrors";
import type { ValidatedNEA } from "../Validated";
import type { ArrayValues } from "../Types";
/**
 * A validator that returns {@link Validated} containing
 * the tested value, if the value is in the set of
 * acceptable values provided.
 *
 * If the value passed in is not one of the acceptable
 * values, then
 * {@link Invalid} will be returned with an error of
 * {@link ValueNotExpected}.
 *
 * @internal
 */
export declare const asValueIn: <VS extends readonly unknown[]>(testValues: VS) => <T>(value: T) => ValidatedNEA<T extends ArrayValues<VS> ? never : ValueNotExpected, T extends ArrayValues<VS> ? T : ArrayValues<VS>>;
