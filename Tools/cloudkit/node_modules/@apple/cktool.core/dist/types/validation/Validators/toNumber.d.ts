import BigNumber from "bignumber.js";
import type { Double, Float } from "./Types";
/**
 * Coerce any supported numeric type to a JS number, if possible.
 * If the value passed in is out of the range of the number,
 * {@link ValueNotInNumberRangeError} will be thrown.
 *
 * @param value A numeric value to coerce.
 * @returns A JS number equivalent of the value passed in,
 *     if possible.
 * @internal
 */
export declare const toNumber: (value: number | bigint | BigNumber | Double | Float) => number;
