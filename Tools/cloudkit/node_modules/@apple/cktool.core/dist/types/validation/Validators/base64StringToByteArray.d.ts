import type { Base64String, ByteArray } from "./Types";
import type { ValidatedNEA } from "../Validated";
import { ValueNotBase64StringError } from "../ValidationErrors";
/**
 * Produce a {@link ByteArray} from a known Base64 string.
 * This function is not guaranteed to validate the
 * string passed in. Instead, {@link toBase64String} can
 * be used to validate the value before it is passed
 * to this function.
 *
 * @param value The known Base64 string to generate a
 *     ByteArray for.
 * @internal
 */
export declare let base64StringToByteArray: (value: Base64String) => ValidatedNEA<ValueNotBase64StringError, ByteArray>;
/**
 * Sets the {@link base64StringToByteArray} function.
 * This function is necessary since base64StringToByteArray needs
 * to be specialized depending on the target platform.
 *
 * @param fn The implementation of base64StringToByteArray.
 * @internal
 */
export declare function setBase64StringToByteArrayFn(fn: typeof base64StringToByteArray): void;
