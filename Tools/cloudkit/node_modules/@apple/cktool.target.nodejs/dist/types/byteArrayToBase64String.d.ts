import type { Base64String, Byte, ByteArray, ValidatedNEA, ValueNotByteArrayError } from "@apple/cktool.core";
/**
 * Produce a Base64 string from a known byte array.
 * This function is not guaranteed to validate the
 * array passed in. Instead, {@link toByteArray} can
 * be used to validate the value before it is passed
 * to this function.
 *
 * @param value The known byte array to generate a
 *     Base64 string for.
 */
export declare const byteArrayToBase64String: (value: readonly Byte[] | ByteArray) => ValidatedNEA<ValueNotByteArrayError, Base64String>;
