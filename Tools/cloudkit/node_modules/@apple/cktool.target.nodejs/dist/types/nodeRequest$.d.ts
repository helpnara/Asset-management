/// <reference types="node" />
import type { Source } from "@apple/cktool.core";
import { FetchError, InvalidSecuritySchemeError, RequestOptions, ResponseStreamItem } from "@apple/cktool.core";
import * as http from "http";
import { NodeFormData } from "./createFormData";
/**
 * If something goes wrong with an http/https request,
 * this error will be emitted.
 */
export declare class NodeFetchError extends FetchError {
    readonly requestError: Error;
    readonly request: http.ClientRequest;
    constructor(message: string, requestError: Error, request: http.ClientRequest);
}
/**
 * If the form data could not be processed to be sent
 * in a request, this error will be emitted.
 */
export declare class NodeFormDataError extends FetchError {
    readonly requestError: Error;
    readonly formData: NodeFormData;
    constructor(message: string, requestError: Error, formData: NodeFormData);
}
/**
 * Send a request to the server and process the
 * server response.
 *
 * @param requestOptions See RequestOptions.
 * @returns A stream of ResponseStreamItem objects.
 */
export declare const nodeRequest$: ({ security, securitySchemes, configuration, method, path, headers, body, queryParams, overrideHeaders, }: RequestOptions) => Source<ResponseStreamItem, NodeFormDataError | NodeFetchError | InvalidSecuritySchemeError>;
