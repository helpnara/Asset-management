import { Configuration } from "@apple/cktool.core";
/**
 * A factory function that creates a `Configuration`
 * instance that contains options for
 * which server to communicate with, how to communicate
 * with the server, and how to
 * serialize/deserialize JSON.
 *
 * The configuration object returned will have have
 * reasonable defaults, but the defaults can be overridden
 * by passing the overrides to this function.
 */
export declare const createConfiguration: (params?: Partial<Configuration> | undefined) => Configuration;
