import { CancellablePromise } from "@apple/cktool.core";
import type { CancelledError, EmptyRecord, FetchError, ReactiveStreamError, ValidationError } from "@apple/cktool.core";
import { GetContainersParams, getContainersOp, getTeamsOp } from "./descriptors";
import type { GetContainersOpConsts, GetContainersOpOptions, GetTeamsOpConsts, GetTeamsOpOptions } from "./descriptors";
export declare type GetContainersPOptions = GetContainersOpOptions;
/**
 * Fetches containers for a team.
 *
 * If successful, the result contains a `ContainersResponse` object. For more information about containers, see `Container`.
 *
 * API operation: getContainers
 * API version: 1.3.2
 *
 * @param options.configuration - The configuration object.
 * @param options.security - An optional dictionary of
 *     security scheme names to the API key or cookie
 *     used for authentication
 *     with the ACP server. If not provided and this operation
 *     is called in a browser, cookie authentication will be
 *     used.
 *
 * @returns
 *     A function that
 *     takes {@link GetContainersParams},
 *     and returns a promise
 *     that yields expected
 *     success responses when resolved.
 *     See {@link getContainersOp.responseDictionary} for
 *     expected responses. Responses in the 2xx range will
 *     be considered success responses.
 *     Other responses will cause the promise to reject.
 */
export declare const getContainersP: {
    (options: GetContainersOpOptions): (args: GetContainersParams, optionsOverride?: GetContainersPOptions) => CancellablePromise<GetContainersOpConsts["successTypes"], GetContainersOpConsts["responseErrorTypes"] | ValidationError | FetchError | CancelledError | ReactiveStreamError>;
    readonly descriptor: typeof getContainersOp;
};
export declare type GetTeamsPOptions = GetTeamsOpOptions;
/**
 * Fetches a list of teams the current user is in.
 *
 * If successful, the result contains a `TeamsResponse` object. For more information about teams, see `Team`.
 *
 * API operation: getTeams
 * API version: 1.3.2
 *
 * @param options.configuration - The configuration object.
 * @param options.security - An optional dictionary of
 *     security scheme names to the API key or cookie
 *     used for authentication
 *     with the ACP server. If not provided and this operation
 *     is called in a browser, cookie authentication will be
 *     used.
 *
 * @returns
 *     A function that
 *     has no parameters
 *     and returns a promise
 *     that yields expected
 *     success responses when resolved.
 *     See {@link getTeamsOp.responseDictionary} for
 *     expected responses. Responses in the 2xx range will
 *     be considered success responses.
 *     Other responses will cause the promise to reject.
 */
export declare const getTeamsP: {
    (options: GetTeamsOpOptions): (args?: EmptyRecord, optionsOverride?: GetTeamsPOptions) => CancellablePromise<GetTeamsOpConsts["successTypes"], GetTeamsOpConsts["responseErrorTypes"] | ValidationError | FetchError | CancelledError | ReactiveStreamError>;
    readonly descriptor: typeof getTeamsOp;
};
