import * as discriminator_ from "./discriminator/index";
import * as makeRecordFieldValue_ from "./make/index";
export * from "./export-types/index";
/**
 * Factory functions that create record field values.
 *
 * @example
 *   // To create a string list value
 *   const fv = CKRecordFieldValue.make.list.string(["a", "b"]);
 */
export declare const make: typeof makeRecordFieldValue_;
/**
 * Discriminator values for {@link CKDBRecordFieldValue} objects.
 * A discriminator is the value that is used in the type field of
 * CKDBRecordFieldValue objects.
 *
 * @internal
 * @example
 *   // To access the string list value type discriminator
 *   const td = CKRecordFieldValue.discriminator.list.string;
 */
export declare const discriminator: typeof discriminator_;
