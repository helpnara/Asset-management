import type { CKDBRecordFieldEncryptedLocationValue } from "@apple/cktool.api.database";
import type { ConvertibleToCKDBLocation } from "../utils/index";
/**
 * Creates a {@link CKDBRecordFieldEncryptedLocationValue} from the
 * value passed in.
 * If the value passed in is not valid, an exception will be thrown.
 *
 * @param value A {@link CKDBLocation}.
 * @returns A {@link CKDBRecordFieldEncryptedLocationValue}, if successful.
 */
export declare const makeCKDBRecordFieldEncryptedLocationValue: (value?: ConvertibleToCKDBLocation | undefined) => CKDBRecordFieldEncryptedLocationValue;
