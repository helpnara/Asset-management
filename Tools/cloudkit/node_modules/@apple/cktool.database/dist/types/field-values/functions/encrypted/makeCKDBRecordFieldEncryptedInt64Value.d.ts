import type { CKDBRecordFieldEncryptedInt64Value } from "@apple/cktool.api.database";
import type { ConvertibleToInt64 } from "@apple/cktool.core";
/**
 * Wraps a value, if allowed, to a {@link CKDBRecordFieldEncryptedInt64Value}
 * object.
 * If not allowed, an exception will be thrown.
 *
 * @param value The value to coerce to a {@link CKDBRecordFieldEncryptedInt64Value}.
 *     Number, bigint, and numeric strings can be coerced.
 * @returns The value wrapped in a {@link CKDBRecordFieldEncryptedInt64Value},
 *     if successful.
 */
export declare const makeCKDBRecordFieldEncryptedInt64Value: (value?: ConvertibleToInt64 | undefined) => CKDBRecordFieldEncryptedInt64Value;
